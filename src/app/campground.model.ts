export class Campground {
  constructor (public name: string, public location: string, public description: string, public amenities: string, public comments: string, public rating: string, public images: string) { }
}
